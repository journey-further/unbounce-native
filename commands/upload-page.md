---
name: upload-page
description: Upload an .unbounce file into an Unbounce account as an unpublished page, and verify it
argument-hint: [path to the .unbounce file]
---

Invoke the `upload-page` skill and follow it. Read its `SKILL.md` before you do anything else.

Every upload is `publish: false`. Publishing is the client's decision, in the Unbounce UI.

$ARGUMENTS
